{
  "name": "Newspaper Article Extraction",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "hostRequirements": {
    "cpus": 4,
    "memory": "16gb"
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y tesseract-ocr && pip install anthropic pymupdf pytesseract pillow jupyter transformers 'datasets==2.16.1' torch seqeval accelerate",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-toolsai.jupyter"
      ],
      "settings": {
        "python.languageServer": "None"
      }
    }
  },
  "secrets": {
    "ANTHROPIC_API_KEY": {
      "description": "Your Anthropic API key. Get one at https://console.anthropic.com",
      "documentationUrl": "https://console.anthropic.com"
    }
  }
}
